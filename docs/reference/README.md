# Reference assets

- `FBFM_ColorScheme.csv` and `FBFM_ColorScheme.xlsx` provide a display color scheme for Fire Behavior Fuel Model values.
- `FireApp_VariableList.xlsx` documents FireBehaviorModels application variables.

These files are user reference material. The `flame_components` package does not read either asset at runtime.